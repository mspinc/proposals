<main class="content">
  <div class="container-fluid">
    <div class="header">
      <h1 class="header-title">
        Proposals
      </h1>
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
           <li class="breadcrumb-item"> <%= link_to "Dashboard", dashboards_path %> </a></li>
          <li class="breadcrumb-item active" aria-current="page">Proposals</li>
          <li class="breadcrumb-item active" aria-current="page">New</li>
        </ol>
      </nav>
    </div>
    <div class="row">
      <div class="col-12">
        <div class="card">
          <div class="card-body">
            <div class="m-sm-4">
              <%= form_for([@proposal,@invite]) do |f| %>
                <div class="mb-3">
                  <label>First Name</label>
                  <%= f.text_field :firstname, autofocus: true, class:'form-control' %>
                </div>
                <div class="mb-3">
                  <label>Last Name</label>
                  <%= f.text_field :lastname, class:'form-control' %>
                </div>
                <div class="mb-3">
                  <label>Email</label>
                  <%= f.email_field :email, class:"form-control form-control-lg", placeholder:"Enter your email", autocomplete: "email" %>
                </div>
                <div>
                  <label>Invite As</label>
                  <%= f.select(:invited_as, options_for_select([['Select One:', ''], ['Participant', 'Participant'], ['Co-organiser', 'Co-organiser']]), {}, {class: "form-control"}) %>
                </div>
                <div class="text-center mt-3">
                  <%= f.submit "Send", class: 'btn btn-lg btn-primary' %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>
